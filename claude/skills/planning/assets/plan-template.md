# Plan: <task description>

## Goal
<!-- Example: "Add conditional mutation support to HBase handler" -->
<!-- Example: "Identify root cause of replication lag on RG-3" -->

## Success Criteria
<!-- Example: "All existing tests pass + new MTR test for conditional mutation" -->
<!-- Example: "Root cause identified, RCA report written with remediation steps" -->
- [ ] ...

## Steps
<!-- Mark: [ ] todo, [~] in-progress, [x] done -->
<!-- Example (feature dev — follow this exact sequence; see planning SKILL.md § "Feature Development: the canonical step sequence"):
1. [x] Clarify requirements → invoke `requirements-clarification` (fills spec.md §1-3) → Gate 1 ✅
2. [~] Design solution → invoke `system-design` (fills spec.md §4+) → Gate 2
3. [ ] Break into tasks → invoke `task-planning` (produces tasks.md)
4. [ ] Implement (prefer TDD; judge granularity — per task or whole-feature suite up front); run full test suite after each task goes green
5. [ ] Code review → invoke `code-review` → Gate 6
6. [ ] Submit MR → invoke `submit-mr` → Gate 7
-->
<!-- Example (bug fix — prefer red-green: write the failing repro test first):
1. [x] Investigate and reproduce the bug
2. [~] Write a failing regression test that pins the bug down
3. [ ] Implement the fix until the test goes green
4. [ ] Run full test suite; confirm no collateral damage
5. [ ] Code review → invoke `code-review` → Gate 6
6. [ ] Submit MR → invoke `submit-mr` → Gate 7
-->
<!-- Example (RCA):
1. [x] Reproduce the issue
2. [~] Trace code path, analyze logs
3. [ ] Identify root cause
4. [ ] Write RCA report
-->

1. [ ] ...
